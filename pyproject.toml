[project]
name = "engine-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "diffusers>=0.31.0",
    "numpy>=2.1.3",
    "prefect>=3.1.6",
    "torch>=2.5.1",
]
